import type { Wings } from "../structures/Wings";
import type { Connection } from "ssh2";

export function createSsh2Client(wings: Wings, client: Connection) {
  // TODO: Add event listeners
  throw new Error("This adapter hasn't been created yet.");
}
